{% extends 'base.html.twig' %}

{% block title %}Editar edad{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-6">
            <h2>Editar edad</h2>
            <form action="" method="post" class="mb-2">
                {% include 'common/field.html.twig' with
                    {type: 'text', entity: 'age', name: 'name', label: 'Nombre', value: age.name, required: true} only %}

                {% include 'common/field.html.twig' with
                    {type: 'text', entity: 'age', name: 'description', label: 'Descripción', value: age.description} only %}

                {% include 'common/field.html.twig' with
                    {type: 'text', entity: 'age', name: 'url', label: 'URL', value: age.url} only %}

                {% if canEdit %}
                    <button type="submit" class="btn btn-primary btn-block">Editar</button>
                    <a href="{{ path('age_delete', {age_id: age.id|encodeUuid}) }}" class="btn btn-danger btn-block confirmable">Borrar</a>
                {% endif %}
            </form>
            <a href="{{ path('age_index') }}">Volver a la lista</a>
        </div>
    </div>
{% endblock %}
