{% extends 'base.html.twig' %}

{% block title %}Editar nivel{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-6">
            <form action="" method="post">
                <div class="form-group">
                    <label for="customer-name">Nombre</label>
                    <input id="customer-name" name="name" type="text" class="form-control" value="{{ zone.name }}"/>
                </div>
                <div class="form-group">
                    <label for="customer-name">Descripción</label>
                    <input id="customer-name" name="description" type="text" class="form-control" value="{{ zone.description }}"/>
                </div>
                {% if canEdit %}
                    <button type="submit" class="btn btn-primary btn-block">Editar</button>
                {% endif %}
            </form>
            <a href="{{ path('zone_index') }}">Volver a la lista</a>
        </div>
    </div>
{% endblock %}
