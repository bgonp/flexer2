{% extends 'base.html.twig' %}

{% block title %}Editar disciplina{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-6">
            <form action="" method="post">
                <div class="form-group">
                    <label for="customer-name">Nombre</label>
                    <input id="customer-name" name="name" type="text" class="form-control" value="{{ discipline.name }}"/>
                </div>
                <div class="form-group">
                    <label for="customer-name">Descripción</label>
                    <input id="customer-name" name="description" type="text" class="form-control" value="{{ discipline.description }}"/>
                </div>
                <div class="form-group">
                    <label for="customer-name">URL</label>
                    <input id="customer-name" name="url" type="text" class="form-control" value="{{ discipline.url }}"/>
                </div>
                {% if canEdit %}
                    <button type="submit" class="btn btn-primary btn-block">Editar</button>
                {% endif %}
            </form>
            <a href="{{ path('discipline_index') }}">Volver a la lista</a>
        </div>
    </div>
{% endblock %}
