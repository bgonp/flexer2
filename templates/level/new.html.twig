{% extends 'base.html.twig' %}

{% block title %}Crear nivel{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-6">
            <h2>Crear nivel</h2>
            <form action="" method="post" class="mb-2">
                {% include 'common/field.html.twig' with
                    {type: 'text', entity: 'level', name: 'name', label: 'Nombre', value: name, required: true} only %}

                {% include 'common/field.html.twig' with
                    {type: 'text', entity: 'level', name: 'description', label: 'Descripción', value: description} only %}

                {% include 'common/field.html.twig' with
                    {type: 'text', entity: 'level', name: 'url', label: 'URL', value: url} only %}


                <button type="submit" class="btn btn-primary btn-block">Guardar</button>
            </form>
            <a href="{{ path('level_index') }}">Volver a la lista</a>
        </div>
    </div>
{% endblock %}
